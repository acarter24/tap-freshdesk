{
    // launch.json
    "version": "0.2.0",
    "configurations": [
      
      {
        "name": "tap-freshdesk discovery",
        "type": "python",
        "request": "launch",
        "module": "tap_freshdesk.tap",
        "args": ["--config", ".secrets/config.json", "--state", ".secrets/state.json"],
        "python": "${command:python.interpreterPath}",
        // Set to true to debug third-party library code
        "justMyCode": true,
      }
    ]
  }